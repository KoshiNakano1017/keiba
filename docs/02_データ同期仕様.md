# データ同期仕様

**最終更新:** 2026-03-27  
**ステータス:** 一部未定（打ち合わせ 2026-03-28 にて確定予定）

---

## 1. データフロー全体図

```
CrossFactor（Windows）
    └─ SQLite DB
        └─ Python抽出スクリプト（バッチ）
            └─ Supabase（クラウドDB）
                └─ Next.js / Vercel（Web）
                    └─ ユーザー（ブラウザ）
```

## 2. ソース（Windows側）

| 項目 | 内容 | ステータス |
|------|------|----------|
| DBの種類 | CrossFactor 独自 SQLite | **確定** |
| データソース数 | 10サービスの競馬データ＋独自映像解析データ | **確定** |
| 独自データ例 | レース中の不利・距離ロス・走行位置・トラックバイアス | **確定（概念）** |
| 対象テーブル | 未定（CrossFactor側のスキーマ確認後に確定） | 未定 |
| 抽出カラム一覧 | 未定（データ・マッピング表にて整理予定） | 未定 |

## 3. 抽出・転送スクリプト（Python）

| 項目 | 内容 | ステータス |
|------|------|----------|
| 言語 | Python | **確定** |
| 実行方式 | Windowsタスクスケジューラ（cron相当） | **確定（方針）** |
| 転送先 | Supabase REST API または Supabase Python Client | **確定（方針）** |
| 実行頻度 | 未定（レースデータ更新タイミングが確定したら決まる） | 前日・当日・レース前 |
| 差分更新 vs 全量 | 未定（データ量とSupabaseプランが確定したら決まる） | 未定 |
| エラー通知 | 未定（Slack / メール / ログのみ） | 未定 |、レスポンシブ。スマホ対応、管理画面も追加

## 4. クラウドDB（Supabase）

| 項目 | 内容 | ステータス |
|------|------|----------|
| サービス | Supabase | **確定** |
| 主な用途 | 認証・DB・権限管理 | **確定** |
| テーブル設計 | 未定（打ち合わせ後に設計開始） | 未定 |
| RLS（行レベルセキュリティ） | プラン別アクセス制御に使用予定 | **確定（方針）** |
| データ鮮度要件 | 未定（レース当日に間に合えばOKか、前日夜でOKか） | 未定（Shibata様の運用ワークフロー確認後） |

## 5. AIモデルとの連携

| 項目 | 内容 | ステータス |
|------|------|----------|
| 学習実行場所 | Windowsローカル（Python） | **確定（方針）** |
| 推論結果の保存先 | Supabase（予想勝率・期待値等のカラム） | **確定（方針）** |
| 再学習頻度 | 未定（週次 / 月次 / 随時） | 未定（打ち合わせで決まる） |
| オッズデータの取得 | 未定（どのAPIまたはデータソースを使うか） | 未定（CrossFactorの連携サービス確認後） |

## 6. データ・マッピング表（下書き）

> CrossFactorのカラム → 特徴量名 → Webでの表示カラム → 対象プラン  
> 詳細は打ち合わせ後に `02_データマッピング表.xlsx` として別途作成

| CrossFactor項目（仮） | 特徴量名（仮） | Web表示名 | 対象プラン |
|----------------------|-------------|---------|----------|
| 過去着順 | 近走成績 | （内部特徴量として利用） | 非表示 |
| 距離ロス | distance_loss | （未定） | 未定 |
| トラックバイアス | track_bias | （未定） | 未定 |
| レース中の不利 | interference | （未定） | 未定 |
| 調教データ | training_eval | （未定） | 未定 |
| 予想勝率（モデル出力） | win_prob | 予想勝率 | 未定 |
| 期待値（モデル出力） | expected_value | 期待値 | 未定（プロプランを想定） |

---

## 未決事項一覧

| # | 未決事項 | トリガー |
|---|---------|---------|
| 1 | 対象テーブルとカラム一覧 | CrossFactor側スキーマ共有後 |
| 2 | 転送頻度（リアルタイム/日次/週次） | Shibata様の運用ワークフロー確認後 |
| 3 | 差分更新 vs 全量更新方針 | データ量確認後 |
| 4 | エラー通知方法 | 打ち合わせ 2026-03-28 |
| 5 | Supabaseテーブル設計 | 打ち合わせ後に着手 |
| 6 | 再学習頻度 | 前日・当日・レース前 |
| 7 | データ鮮度要件（何時までに更新必要か） | Shibata様確認後 |
